import { LineRender } from './Line';
export declare type Direction = "left" | "right" | "top" | "bottom";
/**
 * notice: only for circle node, cause the force line is render by circle-node's radius
 */
declare const ForceLine: LineRender;
export default ForceLine;
